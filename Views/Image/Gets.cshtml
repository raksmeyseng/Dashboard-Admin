@model List<ArchtistStudio.Modules.Image.ListImageResponse>

<div>
    <h4 class="font-weight-bold py-3 mb-4">
        Information Image
    </h4>
    <div class="text-end m-2">
        <a class="text-decoration-none btn btn-outline-dark" href="/imageshow/gets">Slide Show</a>
        <a class="text-decoration-none btn btn-outline-dark" href="/project/gets"> Back to Project</a>
    </div>
    <div class="list-group list-group-flush" style="max-height: 80vh; overflow-y: auto;">
        <table class="table table-Secondary table-striped">
            <thead>
                <tr>
                    <th scope="col">No</th>
                    <th scope="col">ProjectName</th>
                    <th scope="col">ImagePath</th>
                    <th scope="col">Action</th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null && Model.Any())
                {
                    int index = 1;
                    foreach (var item in Model)
                    {
                        <tr>
                            <th scope="row">@index</th>
                            <td>@Html.DisplayFor(model => item.Project.ProjectName)</td>
                            <td>@Html.DisplayFor(model => item.ImagePath)</td>
                            <td>
                                <a class="text-decoration-none btn btn-outline-dark" asp-controller="imageshow"
                                    asp-action="insert" asp-route-id="@item.Id"><i class="fa fa-plus-square p-1" aria-hidden="true"></i>SlideShow</a>
                                <a asp-controller="image" asp-action="update"
                                    asp-route-id="@item.Id">
                                    <i class="fa fa-edit fa-lg text-info p-2" aria-hidden="true"></i> 
                                </a>
                                <button type="button" class="btn btn-link text-danger" data-bs-toggle="modal"
                                    data-bs-target="#deleteModal-@item.Id">
                                       <i class="fa fa-trash" aria-hidden="true"></i>
                                </button>
                                <div class="modal fade" id="deleteModal-@item.Id" tabindex="-1"
                                    aria-labelledby="deleteModalLabel-@item.Id" aria-hidden="true">
                                    <div class="modal-dialog">
                                        <div class="modal-content">
                                            <div class="modal-header">
                                                <h1 class="modal-title fs-5" id="deleteModalLabel-@item.Id">Confirm Deletion
                                                </h1>
                                                <button type="button" class="btn-close" data-bs-dismiss="modal"
                                                    aria-label="Close"></button>
                                            </div>
                                            <div class="modal-body">
                                                Are you sure you want to delete this item?
                                            </div>
                                            <div class="modal-footer">
                                                <form asp-controller="image" asp-action="delete" asp-route-id="@item.Id"
                                                    method="post" style="display: inline;">
                                                    @Html.AntiForgeryToken()
                                                    <input type="hidden" name="_method" value="delete" />
                                                    <button type="submit" class="btn btn-danger">Delete</button>
                                                </form>
                                                <button type="button" class="btn btn-secondary"
                                                    data-bs-dismiss="modal">Close</button>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </td>
                        </tr>
                        index++;
                    }
                }
                else
                {
                    <tr>
                        <td colspan="4" class="text-center">No data available</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

</div>
